### Test Method Rationale

This requirement relies on use of a keyboard to validate access and control of all functionality of the content first by checking use of standard keyboard commands (Tab, Space Bar, Enter, Escape, etc.). If {% if page.source == "Web" %}an interface{% elsif page.source == "Doc" %}a document{% endif %} uses non-standard keyboard commands, the {% if page.source == "Web" %}an interface{% elsif page.source == "Doc" %}document{% endif %} must clearly document the commands and make users aware that the commands exist.

Keyboard access and control includes the ability to navigate to **and** away from interactive content using only a keyboard.
